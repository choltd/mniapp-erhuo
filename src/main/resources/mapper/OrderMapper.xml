<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.erhuo.mapper.OrderMapper">


    <insert id="insert" parameterType="com.erhuo.pojo.Order">
        insert into order (pay, buy_user_id, goods_id)
        values (#{pay}, #{buyUserId}, #{goodsId})
    </insert>

</mapper>